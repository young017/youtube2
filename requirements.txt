# FastAPI 서버
fastapi
uvicorn
pydantic>=2.5.0

# Kaggle API (트렌드 기능용)
kaggle>=1.5.12

# YouTube API (트렌드 기능용)
google-api-python-client>=2.0.0

# OpenAI
openai>=1.0.0

# 데이터 처리
pandas>=2.1.0
numpy>=1.24.0

# ML 모델 (선택사항 - 사용하는 모델만 설치)
# catboost: Python 3.13 호환성 문제가 있을 수 있음
# 설치 실패 시 개별 설치: pip install catboost --no-build-isolation
# 또는 Python 3.11/3.12 사용 권장
catboost>=1.2.2
lightgbm>=4.1.0
xgboost>=2.0.0

# 자연어 처리
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Hugging Face Hub (모델 다운로드)
huggingface_hub>=0.30.0

# 데이터베이스 (SQLite는 기본 포함)
# sqlite3는 Python 표준 라이브러리

# 보안 및 암호화
# hashlib, secrets는 Python 내장 모듈

# 유틸리티
python-multipart>=0.0.6
python-dotenv

# 개발 및 테스트 (선택사항)
pytest==7.4.2
